# ClickRegen Web (Next.js)

Mobile-first UI with one primary button and deterministic state transitions.

- Connect wallet (injected provider)
- Submit intent to Portal
- Show counts and education panel

Set env in `.env.local` (see `.env.example`).
