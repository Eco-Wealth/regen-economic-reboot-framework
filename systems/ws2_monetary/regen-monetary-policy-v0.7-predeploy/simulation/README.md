# Simulation

Offline model code used to validate WS2 monetary mechanics assumptions.

Run the deep audit:

```bash
python regen_validation_audit_v0.7.py
```

Outputs are written to `../exports/`.

For deploy readiness, replace synthetic feeds with real chain telemetry and verified ecological metrics.
