# Proto generation notes

When moved into the chain repo, generate Go code from `proto/ws2controller/v1/*.proto` using the repo’s standard buf/protoc workflow.

This pack does not enforce a specific toolchain.
