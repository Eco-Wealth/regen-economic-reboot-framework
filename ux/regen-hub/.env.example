# Regen API
# When set, Next will proxy client calls from `/regen-api/*` -> `${REGEN_API_BASE_URL}/*`
REGEN_API_BASE_URL=http://localhost:8080

# KOI / GaiaAI mentor
KOI_API_BASE_URL=http://localhost:8090
# Optional. Keep server-side only (do NOT prefix with NEXT_PUBLIC_)
KOI_API_KEY=
# Path at KOI_API_BASE_URL for chat requests
KOI_API_CHAT_PATH=/chat

# Offline development / demo mode
# When enabled, the UI uses deterministic mock data instead of calling live APIs.
NEXT_PUBLIC_MOCK_DATA_MODE=0
